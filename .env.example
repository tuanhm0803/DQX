# DQX Database Configuration
# To switch to Oracle, change DB_TYPE to "oracle" and update connection settings

# Database Type: postgresql or oracle
DB_TYPE=postgresql

# Database Connection Settings
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password

# PostgreSQL specific (when DB_TYPE=postgresql)
DB_PORT=5432
DB_NAME=dqx

# Oracle specific (when DB_TYPE=oracle) 
# Uncomment and set these when switching to Oracle:
# DB_PORT=1521
# DB_SERVICE=XEPDB1

# Note: Oracle thin mode has timezone limitations
# This application handles timezone issues by using naive datetime objects
# and SYSDATE instead of NOW() for Oracle compatibility

# JWT Configuration
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
